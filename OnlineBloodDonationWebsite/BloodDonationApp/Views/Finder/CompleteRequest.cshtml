@*@model BloodDonationApp.Models.BloodBankStockMV

    @{
        ViewBag.Title = "Complete";
    }

    <h2>Request For Blood</h2>

    <div class="col-md-6">
        <div class="contact-form-block">
            <h2 class="contact-title"> Complete Request For Blood</h2>


            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()


                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                @Html.HiddenFor(model => model.BloodBankID)
                @Html.HiddenFor(model => model.BloodGroupID)
                @Html.HiddenFor(model => model.BloodBankStockID)

                <h4>Blood Bank Details</h4>
                <hr />
                <dl class="dl-horizontal">
                    <dt>
                        Blood Bank
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.BloodBank)
                    </dd>
                    <dt>
                     Required Blood Group
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.BloodGroup)
                    </dd>
                    </dl>
                    <hr/>

                    <div class="form-group">

                        Enter Blood Quantity(Ltr)
                        @Html.EditorFor(model => model.Quantity, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.Quantity, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group">

                        <input type="submit" style="float:right;" value="Complete Request" class="btn btn-theme" />
                    </div>

                    }

            </div>
    </div>*@


@model BloodDonationApp.Models.BloodBankStockMV

@{
    ViewBag.Title = "Complete";
}

<style>
    .contact-form-block {
        background: #fff;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        margin-top: 20px;
    }

    .contact-title {
        font-size: 28px;
        margin-bottom: 25px;
        color: #333;
        font-weight: 600;
        text-align: center;
    }

    .btn-theme {
        background-color: #28a745; /* Green color */
        color: #fff;
        border: none;
        padding: 10px 20px;
        border-radius: 5px;
        font-size: 16px;
        transition: background-color 0.3s ease;
    }

        .btn-theme:hover {
            background-color: #218838; /* Darker green on hover */
        }

    .form-control {
        border-radius: 5px;
        border: 1px solid #ddd;
        padding: 10px 15px;
        font-size: 16px;
        transition: border-color 0.3s ease;
    }

        .form-control:focus {
            border-color: #007bff;
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
        }

    .text-danger {
        font-size: 14px;
        color: #dc3545;
    }

    hr {
        border-top: 1px solid #ddd;
    }

    dt {
        font-weight: 600;
        color: #555;
    }

    dd {
        margin-bottom: 10px;
    }
</style>
<br />
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="contact-form-block">
                <h2 class="contact-title">Complete Request For Blood</h2>

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    @Html.HiddenFor(model => model.BloodBankID)
                    @Html.HiddenFor(model => model.BloodGroupID)
                    @Html.HiddenFor(model => model.BloodBankStockID)

                    <h4>Blood Bank Details</h4>
                    <hr />
                    <dl class="row">
                        <dt class="col-sm-4">Blood Bank</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.BloodBank)</dd>

                        <dt class="col-sm-4">Required Blood Group</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.BloodGroup)</dd>
                    </dl>
                    <hr />

                    <div class="form-group">
                        <label>Enter Blood Quantity (Ltr)</label>
                        @Html.EditorFor(model => model.Quantity, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.Quantity, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group text-right">
                        <input type="submit" value="Complete Request" class="btn btn-theme" />
                    </div>
                }
            </div>
        </div>
    </div>
</div>