@*@model IEnumerable<DatabaseLayer.UserTable>

    @{
        ViewBag.Title = "AllNewUserRequests";
    }

    <div class="col-md-12">
        <div class="contact-form-block">
            <h2 class="contact-title">All New User Request</h2>

            <table class="table table-hover">
                <tr>
                    <th>
                     Relationship
                    </th>
                    <th>
                        User Name
                    </th>
                    <th>
                       Email Address
                    </th>
                    <th>
                   Message
                    </th>
                    <th>
                        Account Status
                    </th>

                    <th>Action</th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.UserTypeTable.UserType)
                        </td>
                        <td>
                           @Html.ActionLink( item.UserName, "UserDetails",new {id=item.UserID})
                        </td>

                        <td>
                            @Html.DisplayFor(modelItem => item.EmailAddress)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Description)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.AccountStatusTable.AccountStatus)
                        </td>

                        <td>
                            @Html.ActionLink("Approved", "UserApproved", new { id = item.UserID }, new { @class = "btn btn-primary" })
                            @Html.ActionLink("Rejected", "UserRejected", new { id = item.UserID }, new { @class = "btn btn-danger" })

                        </td>
                    </tr>
                 }

            </table>
            </div>
    </div>*@

@model IEnumerable<DatabaseLayer.UserTable>

@{
    ViewBag.Title = "AllNewUserRequests";
}

<style>
    .contact-form-block {
        background: #fff;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        margin-top: 20px;
    }

    .contact-title {
        font-size: 28px;
        margin-bottom: 25px;
        color: #333;
        font-weight: 600;
        text-align: center;
    }

    .table-responsive {
        overflow-x: auto;
    }

    .table-hover tbody tr:hover {
        background-color: #f8f9fa;
    }

    .table th, .table td {
        vertical-align: middle;
        white-space: nowrap;
    }

    .btn-primary {
        background-color: #28a745;
        border-color: #28a745;
        color: #fff;
        padding: 8px 16px;
        font-size: 14px;
        border-radius: 5px;
        transition: background-color 0.3s ease;
    }

        .btn-primary:hover {
            background-color: #218838;
            border-color: #218838;
        }

    .btn-danger {
        background-color: #dc3545;
        border-color: #dc3545;
        color: #fff;
        padding: 8px 16px;
        font-size: 14px;
        border-radius: 5px;
        transition: background-color 0.3s ease;
    }

        .btn-danger:hover {
            background-color: #c82333;
            border-color: #c82333;
        }

    .text-danger {
        font-size: 14px;
        color: #dc3545;
    }

    a {
        color: #007bff;
        text-decoration: none;
        transition: color 0.3s ease;
    }

        a:hover {
            color: #0056b3;
            text-decoration: underline;
        }
</style>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-12">
            <div class="contact-form-block">
                <h2 class="contact-title">All New User Requests</h2>

                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Relationship</th>
                                <th>User Name</th>
                                <th>Email Address</th>
                                <th>Message</th>
                                <th>Account Status</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(modelItem => item.UserTypeTable.UserType)</td>
                                    <td>@Html.ActionLink(item.UserName, "UserDetails", new { id = item.UserID })</td>
                                    <td>@Html.DisplayFor(modelItem => item.EmailAddress)</td>
                                    <td>@Html.DisplayFor(modelItem => item.Description)</td>
                                    <td>@Html.DisplayFor(modelItem => item.AccountStatusTable.AccountStatus)</td>
                                    <td>
                                        @Html.ActionLink("Approved", "UserApproved", new { id = item.UserID }, new { @class = "btn btn-primary" })
                                        @Html.ActionLink("Rejected", "UserRejected", new { id = item.UserID }, new { @class = "btn btn-danger" })
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>